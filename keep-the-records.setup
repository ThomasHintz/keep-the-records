;; ;; Compile the extension
;; (compile -S -s -O3 -d2 -j awful awful.scm)
;; (compile -S -s -O3 -d2 awful.import.scm)

;; ;; Compile the application (server)
;; (compile -S -O3 -d2 awful-server.scm -o awful)

;; (install-extension 'awful
;;                    '("awful.so" "awful.import.so")
;;                        '((version "0.35")))

;; (install-program 'awful-server
;;                  '("awful")
;;                  `((version "0.35")))


(compile -s -S -O3 -d2 utils.scm)
(compile -s -S -O3 -d2 production.scm)
(compile -s -S -O3 -d2 macs.scm)
(compile -s -S -O3 -d2 storage-funcs.scm)
(compile -s -S -O3 -d2 pdf.scm)
(compile -s -S -O3 -d2 handlers.scm)
(compile -s -S -O3 -d2 rest.scm)
(compile -s -S -O3 -d2 demo-data.scm)
(compile -s -S -O3 -d2 sections.scm)
(compile -s -S -O3 -d2 awana-data-dsl.scm)
(compile -s -S -O3 -d2 sessions.scm)

(compile -s -S -O3 -d2 keep-the-records.scm)


;; (load "macs")
;; (load "utils")
;; ;(load "mda.scm")
;; (load "mda-client")
;; (load "storage-funcs")
;; (load "pdf")
;; (load "handlers")
;; (load "rest")
;; (load "demo-data")
;; (load "sections")
;; (load "awana-data-dsl")
;; (load "sessions")

;; (load "keep-the-records")
